---
title: "Sesión: Log-2026-06-07"
date: 2026-06-07
tags:
  - log
  - sesion
  - supabase
  - schema
---

# Log de Sesión: 2026-06-07

## 📝 Resumen de la Sesión
Inicio de la migración a Supabase (sub-proyecto 1: "Fundación"). Se diseñó y aprobó el spec del **Schema v2.0 alineado con el UI**, se recreó el proyecto Supabase **desde cero** (`poncebenzo`, ref `eknfqxetigmrteouaqnv`, us-east-1, Postgres 17), se aplicó el schema consolidado vía MCP, y se verificó end-to-end (trigger de anomalías, advisors, handshake Python). Ejecución subagent-driven (implementador + revisor de spec + revisor de calidad por tarea) en la rama `feature/supabase-schema-v2`.

## 🛠️ Cambios Realizados
- **`tools/supabase_schema.sql`**: reescrito como v2.0 consolidado (sin capas `ALTER` históricas). Novedades: tabla `location_pings` (con GEOGRAPHY generada + GiST), `tasks.description` + `status` `open/resolved`, `competition_reports.visit_id`, índice único parcial de encargado por tienda, políticas RLS nuevas (escritura de `stores` para staff, lectura de supervisor en `sessions`/`location_pings`), bucket `visit-photos` + políticas de Storage por carpeta. Hardening del review: trigger `SECURITY DEFINER`, clientes sin INSERT en `tasks`, supervisor solo-lectura en competencia, `REVOKE EXECUTE` de funciones internas.
- **Hub (`types.ts`, `mock-data.ts`, `tareas/page.tsx`, `ActivityFeed.tsx`)**: eliminado el concepto `priority` del UI de tareas (decisión de producto); `Task` alineada a BD (`open/resolved` + `description`); `CompetitionReport.visit_id`; `duration_minutes`/`location_verified` marcados mock-only (sin respaldo en BD, a extinguir al cablear). tsc + lint + build OK.
- **Mobile (`db.ts`, `RouteContext.tsx`, `tasks/locationTask.ts`)**: SQLite local alineado — `competition_reports.visit_id` y `location_pings.user_id` (migraciones idempotentes con helper generalizado); `insertLocationPingSync` resuelve la jornada abierta para poblar `session_id`/`user_id`. tsc OK.
- **Supabase remoto**: schema v2.0 aplicado como migración `schema_v2_0_consolidado` + `harden_function_grants`; idempotencia verificada con segunda ejecución; 11 tablas con RLS; seed de `competitor_brands`; bucket `visit-photos` creado.
- **Entorno**: `hub/.env.local` y `.env` raíz con credenciales reales (gitignored); `hub/.env.example` documenta el `.env` raíz del script de handshake.

## ✅ Verificación
- Trigger de anomalías: INSERT de visita `anomaly` + observaciones → tarea `open` con `task_type='reponer_stock'`, `description` poblada y asignada al supervisor. Datos de prueba limpiados.
- Advisors de seguridad: 0 errores propios. Residuales conocidos: `spatial_ref_sys` sin RLS (tabla interna de PostGIS, no modificable — `must be owner`) y `postgis` en schema `public` (mover requeriría recrear columnas GEOGRAPHY).
- `tools/verify_supabase_connection.py`: handshake completo (env, DB, Storage, Auth) ✅.

## 🤝 Decisiones Tomadas
- *Supabase desde cero con schema consolidado*: ver [[decisiones/ADR-003-Supabase-Desde-Cero|ADR-003]].
- *Tareas sin `priority`*: solo se agregó `description` (el trigger copia las observaciones del check-in); los chips de prioridad se eliminaron del UI.
- *Sin check-out / `duration_minutes`*: el mobile no captura salida de tienda; el campo queda mock-only.
- *Spec del sub-proyecto*: [[arquitectura/Spec - Supabase Schema v2|Spec — Supabase Schema v2.0]] (estado: Implementado).

## 🚀 Próximos Pasos (Para la siguiente sesión)
- [ ] Merge de `feature/supabase-schema-v2` a `master`.
- [ ] Sub-proyecto 2 — **Ingesta**: pipeline idempotente en `tools/import-data.ts` (usuarios Auth con contraseña temporal desde `tools/vendedores.json` con correos reales + tiendas de `MAESTRO.xlsx` con coords provisionales de Caracas + rutas de `RUTAS 05-12-25 (1).xlsx`).
- [ ] Conseguir coordenadas GPS reales de tiendas (bloqueador de negocio restante).
- [ ] Sub-proyectos 3-4: cablear hub y mobile a Supabase.

## 🔗 Enlaces Relacionados
- [[roadmap/Roadmap|Roadmap del Proyecto]]
- [[pendientes/Pendientes|Pendientes]]
- [[arquitectura/Esquema Base Datos|Esquema de Base de Datos]]
