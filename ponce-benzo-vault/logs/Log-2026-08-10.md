---
title: "Sesión: Log-2026-08-10"
date: 2026-08-10
tags:
  - log
  - sesion
  - usuarios
  - deploy
---

# Log de Sesión: 2026-08-10

## 📝 Resumen de la Sesión
Dos temas cortos: (1) alta de **Diego Mori** como segundo usuario `admin` del hub, y (2) decisión de **abandonar el repositorio espejo** de Vercel y desplegar el hub desde el monorepo usando el ajuste *Root Directory*.

## 🛠️ Cambios Realizados
- **Supabase Auth + `public.users`**: creado `dmori@ponce-benzo.com` (Diego Mori, rol `admin`, `supervisor_id = null`, `active = true`, `email_confirm = true`). `id = 680849d5-0265-4287-8d67-ad0453383176`. Login verificado con `grant_type=password` contra la anon key.
- **`tools/vendedores.json`**: añadida la entrada de Diego Mori para que la ingesta (`stageUsers.ts`) sea idempotente y no lo pise en futuras corridas. `excel_aliases` vacío — no aparece en MAESTRO/RUTAS.
- **`tools/vendedores.secret.json`** (no versionado) y **[[Credenciales Temporales Piloto|Credenciales Temporales Piloto]]**: registrada su contraseña temporal.

## 🤝 Decisiones Tomadas
- *Un solo repositorio para el deploy*: se deja de usar el repo espejo `Leofrandex/ponce-benzo-hub`. Vercel se reconectará al monorepo `Leofrandex/ponce-benzo-smart-tracker` con **Root Directory = `hub`**, que era la razón original del espejo (el build se caía al incluir el resto del monorepo). `hub/` tiene su propio `package.json` y `package-lock.json`, así que install y build funcionan aislados sin config extra.
- *El cambio lo ejecuta el usuario en el dashboard*: el MCP de Vercel disponible es de solo lectura para configuración (no expone cambio de repo Git ni de Root Directory) y la CLI no está instalada.

> [!IMPORTANT]
> Diego Mori es el **segundo `admin`**. Por [[bugs/Registro de Bugs|BUG-025]] el rol `admin` ya tiene visibilidad global, incluidas las fotos de `visit-photos`.

## 🚀 Próximos Pasos (Para la siguiente sesión)
- [ ] Usuario: en Vercel → proyecto `ponce-benzo-hub` → Settings → Git → *Disconnect* del espejo → *Connect* a `ponce-benzo-smart-tracker` (rama `master`) → Build and Deployment → Root Directory = `hub` (sin marcar "Include files outside of the Root Directory").
- [ ] Verificar un redeploy de producción tras el cambio y confirmar que las variables de entorno del proyecto se conservaron.
- [ ] Archivar / marcar como obsoleto el repo espejo `Leofrandex/ponce-benzo-hub` una vez validado.
- [ ] Definir el flujo de cambio de contraseña en el primer ingreso (sigue pendiente desde el piloto).

## 🔗 Enlaces Relacionados
- [[roadmap/Roadmap|Roadmap del Proyecto]]
- [[pendientes/Pendientes|Pendientes]]
- [[Credenciales Temporales Piloto|Credenciales Temporales Piloto]]
